# GSAP Examples

This folder is reserved for GSAP animation examples.

Use one folder per example:

```text
gsap/example-name/index.html
```

Add each new example to the `PROJECTS` array in the root `index.html`.
