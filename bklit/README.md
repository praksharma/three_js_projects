# bklit UI Examples

This folder is reserved for bklit UI examples.

Reference: https://bklit.com/

Use one folder per example:

```text
bklit/example-name/index.html
```

Add each new example to the `PROJECTS` array in the root `index.html`.
